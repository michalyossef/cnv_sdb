// this is not an automated test suite (yet).
// manually executing requires a "local.js" file with user/pass that has access to IAM WS.
// Example: node test.js <token-here> <appId-here>

var local = require('./local.js');

var iam = require('./index')({user: local.iamUser, pass: local.iamPass, scope: 'Token_WindowsAuth'});
console.log(iam);

iam.validateWindowsAuthToken(process.argv[2], process.argv[3])
.then(function() {
		console.log('happy');
	},function() {
		console.log('sad');
	})
